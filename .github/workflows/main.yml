name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  PYTHON_VERSION: '3.10.9'

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - run: |
          ruff check .
          ruff format --check .

  build-and-push-docker:
    name: Build & Push Multi-Arch Images
    runs-on: ubuntu-latest
    needs: lint-and-test
    environment: data_analysis_assistant
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push API
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./app/Dockerfile
          push: true
          platforms: linux/arm64
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/data_analysis_api:latest

      - name: Build & Push Frontend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./streamlit_app/Dockerfile
          push: true
          platforms: linux/arm64
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/data_analysis_frontend:latest

      - name: Build & Push Nginx
        uses: docker/build-push-action@v5
        with:
          context: ./nginx
          file: ./nginx/Dockerfile
          push: true
          platforms: linux/arm64
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/data_analysis_nginx:latest

  deploy:
    name: Deploy to ARM Server
    runs-on: ubuntu-latest
    needs: build-and-push-docker
    environment: data_analysis_assistant

    steps:
      - name: Copy Compose & Nginx
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "docker-compose.yml,nginx/"
          target: "/home/ubuntu/data_analysis_assistant"

      - name: Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.REMOTE_HOST }}
          username: ${{ secrets.REMOTE_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/data_analysis_assistant

            echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

            export DOCKERHUB_USERNAME="${{ secrets.DOCKERHUB_USERNAME }}"
            export SENTRY_DSN="${{ secrets.SENTRY_DSN }}"
            export REDIS_URL="${{ secrets.REDIS_URL }}"
            export DATABASE_URL_ASYNC="${{ secrets.DATABASE_URL_ASYNC }}"
            export DATABASE_URL_SYNC="${{ secrets.DATABASE_URL_SYNC }}"
            export LLM_API_KEY_GOOGLE="${{ secrets.LLM_API_KEY_GOOGLE }}"
            export LLM_API_KEY_CEREBRAS="${{ secrets.LLM_API_KEY_CEREBRAS }}"
            export EMAIL_USERNAME="${{ secrets.EMAIL_USERNAME }}"
            export EMAIL_PASSWORD="${{ secrets.EMAIL_PASSWORD }}"
            export EMAIL_SERVER="${{ secrets.EMAIL_SERVER }}"
            export SECRET_KEY="${{ secrets.SECRET_KEY }}"
            export API_KEY_ENCRYPTION_KEY="${{ secrets.API_KEY_ENCRYPTION_KEY }}"
            export REDIS_PASSWORD="${{ secrets.REDIS_PASSWORD }}"
            export API_URL="${{ secrets.API_URL }}"
            export PUBLIC_URL="${{ secrets.PUBLIC_URL }}"

            docker compose pull
            docker compose run --rm fastapi_app alembic upgrade head
            docker compose up -d
